apiVersion: v1
kind: Service
metadata:
  name: ai-agent-app
  namespace: ai-agent-ops
spec:
  # Expose externally via LoadBalancer for local (Docker Desktop) access on localhost:80
  type: LoadBalancer
