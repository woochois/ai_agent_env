apiVersion: v1
kind: Secret
metadata:
  name: postgres-secrets
  namespace: ai-agent-ops
  labels:
    app: ai-agent-ops
    component: database
    version: v1.0.0
type: Opaque
# NOTE: These are base64-encoded PLACEHOLDER values only.
# Replace them with real, base64-encoded secrets before deploying, e.g.:
#   echo -n 'my-password' | base64
# Keys: POSTGRES_DB, POSTGRES_USER, POSTGRES_PASSWORD
data:
  # placeholder -> "ai_agent"
  POSTGRES_DB: YWlfYWdlbnQ=
  # placeholder -> "ai_agent_user"
  POSTGRES_USER: YWlfYWdlbnRfdXNlcg==
  # placeholder -> "REPLACE_WITH_POSTGRES_PASSWORD"
  POSTGRES_PASSWORD: UkVQTEFDRV9XSVRIX1BPU1RHUkVTX1BBU1NXT1JE
